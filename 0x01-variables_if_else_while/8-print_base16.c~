#include <stdio.h>

/**
 *main- this code prints all the hexadecimal numbers
 *
 *Return: Always 0 (Successful)
 */
int main(void)
{
	char num;
	for (num = '0'; num <= '9'; num++)
		putchar(num);

	for (num = 'a'; num <= 'f'; num++)
		putchar(num);

	putchar('\n');
	return (0);
}
